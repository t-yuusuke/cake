<h3>投稿編集画面</h3>
<%= form_with model: @post do |f| %>
  <h4>タイトル</h4>
    <%= f.text_field :title %>
    <%= f.text_field :body %>
    <%= f.submit '保存する', class: 'btn btn-info rounded-pill' %>
    <% end %>
    <%= link_to "詳細ページに戻る", post_path(@post.id), class: "btn btn-info rounded-pill" %>